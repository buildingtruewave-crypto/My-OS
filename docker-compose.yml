services:
  pulse:
    build: .
    container_name: pulse
    restart: unless-stopped
    ports: ["8501:8501"]
    volumes: ["./data:/app/data"]   # your life, persisted on the VPS
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
